# 环境变量示例
#
# 【重要】这个项目正常使用**不需要**任何环境变量。
# API Key 是你在 App 的「设置」页里填的，只存在手机浏览器本地，不经过服务器。
#
# 这个文件只在你打算「把网址分享给别人用」时才需要 ——
# 那时候不能把 Key 放在前端（静态网页的代码人人可见），
# 要改成用 Cloudflare Worker 代理，Key 放 Worker 的环境变量里。
# 配置方法见 cloudflare-worker.example.js 顶部的说明。

# Worker 里要配的变量（不是这个前端项目要配的）：
# DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx
# ALLOWED_HOSTS=api.deepseek.com,dashscope.aliyuncs.com,open.bigmodel.cn,api.moonshot.cn
# UPSTREAM_BASE=https://api.deepseek.com
