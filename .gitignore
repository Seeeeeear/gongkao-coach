# 依赖与构建缓存
node_modules/
.npm-cache/
*.log

# 本地环境
.env
.env.local
.DS_Store
Thumbs.db

# 编辑器
.vscode/
.idea/

# 备份文件（用户导出的错题备份不要提交）
考公错题备份_*.json

# 第三方 skill 原始文档（仅用于人工审阅与提炼，不进仓库）
#
# 为什么忽略：skills/ 里已经是提炼后的技能包（带来源与许可标注），
# 仓库没必要再存一份第三方原文。需要复核时跑：
#     node scripts/fetch-skills.mjs
# 脚本会先校验许可，只抓 MIT 等宽松许可的仓库。
vendor-skills/
