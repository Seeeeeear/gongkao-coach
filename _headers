# Cloudflare Pages 缓存策略
#
# 部署方式：连接 Git 仓库 → 构建命令留空 → 输出目录填 /
# （产物 app.js / styles.css 已提交在仓库里，线上不需要跑构建）
#
# 这里只保留一条最关键的规则：sw.js 绝不能被缓存。
# 否则你更新了代码，手机上的 Service Worker 还是旧的，会一直加载旧版本，
# 而且自己怎么刷新都没用 —— 这是 PWA 最常见的坑。
#
# 特意只写这一条：_headers 的写法如果有错会导致部署报错，
# 而其他资源的缓存策略收益不大，不值得冒险。

/sw.js
  Cache-Control: no-store

