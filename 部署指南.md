# 部署指南（不用懂技术，照着点）

目标：拿到一个**公网网址**，手机随时随地能打开，不用同一个 WiFi、不用买域名、不用备案。

总耗时约 10 分钟，分两大步：**① 把代码传到 GitHub　② 让 Cloudflare 发布它**。

---

## 前提：先在本机把仓库准备好（已经帮你做完了）

代码已经提交到本地 git 仓库了（提交号 `f2ffa8f`，27 个文件）。

你在**自己的终端**里操作时，如果遇到这个报错：

```
fatal: detected dubious ownership in repository
```

执行一次这个命令就好（把项目路径换成你的实际路径）：

```bash
git config --global --add safe.directory D:/qa/gongkao-coach
```

> 原因：这个目录属于 Administrators，而你的登录用户是 Lenovo，git 出于安全默认拒绝操作。
> 加上这行就是告诉 git「这个目录我信任」。

---

## 第 1 步：把代码传到 GitHub

### 1.1 建一个仓库

1. 打开 <https://github.com/new>
2. **Repository name** 填 `gongkao-coach`
3. 选 **Private（私有）** —— 推荐。这个项目里面没有密钥，但私有更省心
4. **不要**勾选 "Add a README file"（我们已经有 README 了）
5. 点 **Create repository**

### 1.2 把本地代码推上去

建好后 GitHub 会显示一段命令。你只需要在自己终端里执行（**把 `你的用户名` 换成你的 GitHub 用户名**）：

```bash
cd D:\qa\gongkao-coach
git remote add origin https://github.com/你的用户名/gongkao-coach.git
git push -u origin main
```

推送时会弹出登录窗口：

- **推荐用浏览器登录**（GitHub 的 "Sign in with your browser"），点一下授权就行
- 或者用 **Personal Access Token** 当密码（GitHub 密码不能直接用于 git）
- **国内网络**如果卡在连接不动，见文末「网络不通怎么办」

### 1.3 顺手把提交作者改成你自己（可选）

之前的提交用的是占位身份。想改成你的名字和邮箱：

```bash
cd D:\qa\gongkao-coach
git config user.name "你的名字"
git config user.email "你的邮箱"
git commit --amend --reset-author --no-edit
git push -f origin main
```

---

## 第 2 步：让 Cloudflare Pages 发布它

选 Cloudflare 而不是 GitHub Pages 的原因：**国内访问通常更稳**，而且部署更快。

### 2.1 登录 / 注册

打开 <https://dash.cloudflare.com/sign-up> 注册（邮箱即可，免费），登录后进入控制台。

### 2.2 创建 Pages 项目

1. 左侧菜单找到 **Workers & Pages**
2. 点 **Create** → 选 **Pages** 标签 → **Connect to Git**
3. 授权 Cloudflare 访问你的 GitHub（选 "Only select repositories"，只给它 `gongkao-coach` 这一个的权限更安全）
4. 选中 `gongkao-coach` 仓库 → **Begin setup**

### 2.3 ⚠️ 关键设置（这里填错就发布失败）

| 设置项 | 填什么 | 为什么 |
|---|---|---|
| Project name | `gongkao-coach`（随意） | 会变成网址的一部分 |
| Production branch | `main` | |
| Framework preset | **None** | |
| **Build command** | **留空！什么都别填** | 产物（`app.js`、`styles.css`）已经提交在仓库里了，**不需要构建** |
| **Build output directory** | `/` | 因为不需要构建，直接发布仓库根目录 |

> 💡 为什么不用构建：这个项目特意做成了免构建的 —— 源码 `app.jsx` 在开发时就已经编译成 `app.js` 并提交了。好处是**线上不会出现"本地能跑、部署失败"的问题**，而且手机加载更快（不用跑编译）。

点 **Save and Deploy**，等大约 30 秒。

### 2.4 拿到网址

发布完成后，顶部会显示你的网址，形如：

```
https://gongkao-coach.pages.dev
```

在**电脑浏览器**里先打开确认能显示界面。

---

## 第 3 步：装到手机上

1. 手机浏览器打开 `https://gongkao-coach.pages.dev`
2. 浏览器菜单 → **「添加到桌面」** / 「添加到主屏幕」
3. 桌面出现图标，点开就像 App 一样

然后在 App 里：**设置 → 填 DeepSeek API Key → 点「测试连接」**。

> 📷 想用「拍照识题」的话，还要在设置里配一个**能读图的模型**（DeepSeek 不支持读图），
> 设置页有通义千问 VL / 智谱 GLM-4V / Kimi / GPT-4o mini 四个预设，点一下自动填好，
> 再用「测试能否读图」验证。不配也能用「粘贴文字」模式。

---

## 以后改了代码怎么更新

```bash
cd D:\qa\gongkao-coach
npm run build        # 改了 app.jsx 的话，重新编译成 app.js
npm run verify       # 自检（推荐，能抓出白屏/报错）
git add -A
git commit -m "说明改了什么"
git push
```

Cloudflare 会**自动重新发布**，大约 30 秒后生效。

如果手机打开还是旧界面，把 `sw.js` 里的 `VERSION = 'v1'` 改成 `v2` 再推一次（这是离线缓存的版本号）。

---

## 常见问题

**Q: 手机打开是白屏 / 一直转圈？**
先在电脑浏览器打开同一网址确认。如果电脑正常、手机白屏，多半是缓存问题：手机浏览器里清除该站点数据，或换个浏览器试。

**Q: 提示 "Build failed"？**
九成是 Build command 没留空。回到 Cloudflare 的 Settings → Builds & deployments，确认 Build command 是**空的**，Build output directory 是 `/`。

**Q: 担心 API Key 泄露？**
Key 只存在**你自己手机的浏览器里**，不会进代码、不会上传。但**不要**把带 Key 的截图或备份文件发到公开地方。如果将来要把网址给别人用，必须按 README 里说的加一个 Cloudflare Worker 代理（`cloudflare-worker.example.js` 就是模板）。

**Q: 数据会丢吗？**
会 —— 数据只在手机浏览器里。**定期去「设置 → 导出备份」**，把 JSON 存到网盘或微信收藏。

**Q: 国内网络连不上 github.com 怎么办？**
- 开代理后再推送（只有 push 那一下需要）
- 或者不用 GitHub：Cloudflare Pages 也支持 **Direct Upload**（直接上传文件夹），不需要 Git。把整个项目文件夹压缩后从 Pages 的 "Upload assets" 上传即可 —— 但注意**不要**把 `node_modules` 一起打包，否则会很大很慢。

---

## 一句话总结

**推 GitHub → Cloudflare Pages 连仓库 → 构建命令留空、输出目录填 `/` → 拿到 `xxx.pages.dev` → 手机加桌面。**
